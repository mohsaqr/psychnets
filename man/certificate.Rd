% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/certificate.R
\name{certificate}
\alias{certificate}
\title{Correctness certificate of a fitted network}
\usage{
certificate(x, tol = 1e-06)
}
\arguments{
\item{x}{A \link{psychnet} object.}

\item{tol}{Tolerance below which the fit is flagged \code{certified = TRUE}.
Default \code{1e-6}.}
}
\value{
A one-row \code{data.frame} with columns \code{method}, \code{certificate} (the
residual; smaller is better), \code{kind} (\code{"kkt"} for the optimization
certificates, \code{"structural"} for TMFG/relimp, \code{"none"} for cor/pcor), and
\code{certified} (logical: residual at or below \code{tol}).
}
\description{
Every regularized or constrained estimator in \code{psychnet} self-certifies: it
reports how far the returned network sits from the unique optimum of its own
convex objective (a KKT / stationarity residual), or -- for the structural
methods -- whether the graph satisfies the identity that defines it. This
verb returns that certificate as a tidy one-row \code{data.frame}, so correctness
is read the same way for every method.
}
\details{
The residual is near machine zero for a correctly solved problem. \code{cor} and
\code{pcor} have no optimization to certify and report \code{NA}.
}
\examples{
S <- 0.4^abs(outer(1:6, 1:6, "-"))
certificate(ebic_glasso(cor_matrix = S, n = 250))
certificate(tmfg_network(cor_matrix = S))
}
