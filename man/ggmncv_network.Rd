% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggmncv.R
\name{ggmncv_network}
\alias{ggmncv_network}
\title{Non-convex regularized Gaussian graphical model (GGMncv)}
\usage{
ggmncv_network(
  data = NULL,
  cor_matrix = NULL,
  n = NULL,
  penalty = c("atan", "scad", "mcp", "lasso"),
  hyper = NULL,
  gamma = 0.5,
  nlambda = 100L,
  lambda_min_ratio = 0.01,
  threshold = 0,
  labels = NULL
)
}
\arguments{
\item{data}{Numeric data frame or matrix (rows = observations). Optional if
\code{cor_matrix} is supplied.}

\item{cor_matrix}{Optional correlation matrix; if given, \code{n} is required.}

\item{n}{Sample size (required when \code{cor_matrix} is supplied).}

\item{penalty}{\code{"atan"} (default), \code{"scad"}, \code{"mcp"}, or \code{"lasso"}.}

\item{hyper}{Penalty shape hyperparameter (SCAD \code{a}, MCP \code{gamma}, atan
\code{gamma}); per-penalty default if \code{NULL}.}

\item{gamma}{EBIC hyperparameter. Default 0.5.}

\item{nlambda}{Number of penalties on the path. Default 100.}

\item{lambda_min_ratio}{Smallest penalty as a fraction of the largest.}

\item{threshold}{Partial correlations with absolute value below this are
zeroed. Default 0.}

\item{labels}{Optional node labels.}
}
\value{
A \code{psychnet} object whose \verb{$graph} is the partial-correlation matrix,
with \verb{$precision}, \verb{$lambda}, \verb{$gamma}, \verb{$penalty}, \verb{$hyper},
\verb{$penalty_matrix} (the per-edge penalties), \verb{$cor_matrix}, \verb{$ebic}, and
\verb{$kkt}.
}
\description{
Estimates a GGM with a non-convex penalty (SCAD, MCP, or atan) via one-step
local linear approximation, selecting the penalty by EBIC. Non-convex
penalties reduce the shrinkage bias of the L1 (lasso) glasso on strong edges.
Equivalent in purpose to \code{GGMncv::ggmncv()}, but pure base R and self-
certified via \code{\link[=glasso_kkt_weighted]{glasso_kkt_weighted()}}.
}
\examples{
S <- 0.5^abs(outer(1:6, 1:6, "-"))
ggmncv_network(cor_matrix = S, n = 250, penalty = "scad")
}
