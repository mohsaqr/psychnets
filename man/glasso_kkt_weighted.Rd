% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glasso.R
\name{glasso_kkt_weighted}
\alias{glasso_kkt_weighted}
\title{Weighted graphical-lasso stationarity (KKT) residual}
\usage{
glasso_kkt_weighted(theta, cor_matrix, Rho, active_tol = 1e-08)
}
\arguments{
\item{theta}{Precision matrix to test.}

\item{cor_matrix}{Correlation / covariance the model was fit to.}

\item{Rho}{Symmetric p x p matrix of per-edge penalties (diagonal ignored).}

\item{active_tol}{Magnitude above which an off-diagonal entry is "active".}
}
\value{
Maximum absolute stationarity violation (scalar); 0 = exact optimum.
}
\description{
Per-edge-penalty generalization of \code{\link[=glasso_kkt]{glasso_kkt()}}, the certificate for a
non-convex GGM solved by one-step local linear approximation (SCAD / MCP /
atan), where edge \eqn{(i,j)} carries its own penalty \eqn{\rho_{ij}}. With
\eqn{W = \Theta^{-1}}: \eqn{W_{ii} = S_{ii}};
\eqn{W_{ij} - S_{ij} = \rho_{ij}\,\mathrm{sign}(\Theta_{ij})} for active
edges; \eqn{|W_{ij} - S_{ij}| \le \rho_{ij}} otherwise.
}
\examples{
S <- 0.4^abs(outer(1:5, 1:5, "-"))
fit <- ggmncv_network(cor_matrix = S, n = 200)
glasso_kkt_weighted(fit$precision, S, fit$penalty_matrix)
}
