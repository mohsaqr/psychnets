% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/huge.R
\name{huge_network}
\alias{huge_network}
\title{Nonparanormal graphical model (huge)}
\usage{
huge_network(
  data = NULL,
  cor_matrix = NULL,
  n = NULL,
  npn = c("shrinkage", "truncation", "skeptic"),
  gamma = 0.5,
  nlambda = 100L,
  lambda_min_ratio = 0.01,
  threshold = 0,
  labels = NULL
)
}
\arguments{
\item{data}{Numeric data frame or matrix (rows = observations). Optional if
\code{cor_matrix} is supplied (then the transform is skipped).}

\item{cor_matrix}{Optional pre-transformed correlation matrix; if given, \code{n}
is required, \code{data} and \code{npn} are ignored.}

\item{n}{Sample size (required when \code{cor_matrix} is supplied).}

\item{npn}{Nonparanormal transform: \code{"shrinkage"} (default), \code{"truncation"},
or \code{"skeptic"} (Spearman).}

\item{gamma}{EBIC hyperparameter. Default 0.5.}

\item{nlambda}{Number of penalties on the path. Default 100.}

\item{lambda_min_ratio}{Smallest penalty as a fraction of the largest.}

\item{threshold}{Partial correlations with absolute value below this are
zeroed. Default 0.}

\item{labels}{Optional node labels.}
}
\value{
A \code{psychnet} object whose \verb{$graph} is the partial-correlation matrix,
with \verb{$precision}, \verb{$lambda}, \verb{$gamma}, \verb{$cor_matrix} (the transformed
correlation), \verb{$npn}, \verb{$ebic}, and \verb{$kkt}.
}
\description{
Estimates a Gaussian graphical model after a rank-based nonparanormal
transform that relaxes the multivariate-normal assumption, then selects the
L1 penalty by EBIC and refits to the certified optimum. Equivalent in purpose
to \code{huge::huge()} (nonparanormal) / \code{bootnet}'s \code{"huge"} default, but pure
base R and self-certified via \code{\link[=glasso_kkt]{glasso_kkt()}} on the transformed correlation.
}
\examples{
set.seed(1)
x <- matrix(stats::rnorm(300 * 5), 300, 5)
x <- exp(x \%*\% chol(0.4^abs(outer(1:5, 1:5, "-"))))   # break normality
huge_network(x)
}
