% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stability.R
\name{net_stability}
\alias{net_stability}
\title{Centrality-stability coefficient (case-dropping subset bootstrap)}
\usage{
net_stability(
  data,
  method = "glasso",
  measures = c("strength", "expected_influence"),
  centrality_fn = NULL,
  drop_prop = seq(0.1, 0.9, by = 0.1),
  iter = 100L,
  threshold = 0.7,
  certainty = 0.95,
  labels = NULL,
  ...
)
}
\arguments{
\item{data}{Numeric data frame or matrix (rows = observations).}

\item{method}{Estimator (see \code{\link[=psychnet]{psychnet()}}). Default \code{"glasso"}.}

\item{measures}{Centrality measures to assess. Defaults to the two
recommended for psychometric networks (\code{c("strength", "expected_influence")}); \code{"betweenness"}/\code{"closeness"} and custom measures
(via \code{centrality_fn}) are also accepted. See \code{\link[=net_centralities]{net_centralities()}}.}

\item{centrality_fn}{Optional function supplying any non-built-in \code{measures}
(see \code{\link[=net_centralities]{net_centralities()}}).}

\item{drop_prop}{Proportions of cases to drop. Default \code{seq(0.1, 0.9, 0.1)}.}

\item{iter}{Subsets per proportion. Default 100.}

\item{threshold}{Minimum acceptable rank correlation. Default 0.7.}

\item{certainty}{Probability the correlation must exceed \code{threshold}.
Default 0.95.}

\item{labels}{Optional node labels.}

\item{...}{Passed to the estimator.}
}
\value{
An object of class \code{psychnet_stability} with \verb{$cs} (CS-coefficient
per measure) and a tidy \verb{$table} (columns \code{measure}, \code{drop_prop},
\code{mean_cor}, \code{sd_cor}, \code{prop_above}) of the case-dropping correlations by
drop proportion. Visualise it with \code{\link[=plot.psychnet_stability]{plot.psychnet_stability()}}.
}
\description{
Centrality-stability coefficient (case-dropping subset bootstrap)
}
\examples{
set.seed(1)
x <- matrix(stats::rnorm(200 * 5), 200, 5) \%*\% chol(0.4^abs(outer(1:5, 1:5, "-")))
colnames(x) <- paste0("V", 1:5)
cs <- net_stability(x, drop_prop = c(0.3, 0.5, 0.7), iter = 20)
cs$cs
}
