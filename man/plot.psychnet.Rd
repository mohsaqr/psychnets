% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.psychnet}
\alias{plot.psychnet}
\title{Plot a psychnet network}
\usage{
\method{plot}{psychnet}(x, ...)
}
\arguments{
\item{x}{A \code{psychnet} object.}

\item{...}{Passed to \code{\link[cograph:splot]{cograph::splot()}}.}
}
\value{
The value of \code{\link[cograph:splot]{cograph::splot()}}, invisibly.
}
\description{
Renders the estimated network with \code{cograph::splot()} (a Suggested package);
\code{psychnet} objects inherit from \code{cograph_network} for exactly this purpose.
For the bootstrap / centrality / difference / stability diagnostics use the
dedicated \code{plot()} methods for those result objects instead.
}
\examples{
S <- 0.4^abs(outer(1:6, 1:6, "-"))
fit <- ebic_glasso(cor_matrix = S, n = 300)
if (requireNamespace("cograph", quietly = TRUE)) {
  plot(fit)
}
}
