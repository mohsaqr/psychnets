% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.psychnet_bootstrap}
\alias{plot.psychnet_bootstrap}
\title{Plot a network bootstrap}
\usage{
\method{plot}{psychnet_bootstrap}(
  x,
  type = c("edges", "centrality", "edge_diff", "centrality_diff", "predictability"),
  measure = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{psychnet_bootstrap} object from \code{\link[=net_boot]{net_boot()}}.}

\item{type}{One of \code{"edges"}, \code{"centrality"}, \code{"edge_diff"},
\code{"centrality_diff"}, \code{"predictability"}.}

\item{measure}{For \code{"centrality"}/\code{"centrality_diff"}, which measure(s) to
draw. Default: all bootstrapped measures (\code{"centrality"}) or the first
(\code{"centrality_diff"}).}

\item{...}{Unused.}
}
\value{
\code{x}, invisibly. Called for the plot it draws.
}
\description{
Visualises a \code{\link[=net_boot]{net_boot()}} result. \code{type = "edges"} (default) draws the
bootstrapped edge-weight confidence intervals sorted by the observed weight
(bootnet's edge-accuracy plot); \code{type = "centrality"} draws the bootstrapped
centrality intervals, one sorted panel per measure; \code{type = "edge_diff"} and
\code{type = "centrality_diff"} draw the bootstrapped difference "significance box"
matrix for edges or for one centrality; \code{type = "predictability"} draws the
node predictability intervals (only when \code{\link[=net_boot]{net_boot()}} was run with
\code{predictability = TRUE}).
}
\examples{
set.seed(1)
x <- matrix(stats::rnorm(150 * 5), 150, 5) \%*\% chol(0.4^abs(outer(1:5, 1:5, "-")))
colnames(x) <- paste0("V", 1:5)
bs <- net_boot(x, n_boot = 50, cores = 1)   # n_boot >= 1000 for real use
plot(bs)                       # edge-weight CIs
plot(bs, type = "centrality")  # centrality CIs
}
