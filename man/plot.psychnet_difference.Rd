% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.psychnet_difference}
\alias{plot.psychnet_difference}
\title{Plot a bootstrapped difference test}
\usage{
\method{plot}{psychnet_difference}(x, style = c("box", "forest"), ...)
}
\arguments{
\item{x}{A \code{psychnet_difference} data frame from \code{\link[=difference_test]{difference_test()}}.}

\item{style}{\code{"box"} (default) for the significance-box matrix, or
\code{"forest"} for a forest plot of the pairwise differences with their CIs.}

\item{...}{Unused.}
}
\value{
\code{x}, invisibly. Called for the plot it draws.
}
\description{
Draws the bootnet-style "significance box" matrix for the pairwise difference
test returned by \code{\link[=difference_test]{difference_test()}}: items on both axes ordered by their
observed value, the diagonal showing the observed value, and each off-diagonal
cell filled when that pair differs significantly (red when the row item is
larger, blue when smaller). \code{style = "forest"} instead draws a forest plot:
one row per pair, the bootstrapped difference as a point with its confidence
interval, a reference line at zero, and significant pairs (interval excluding
zero) emphasised.
}
\examples{
set.seed(1)
x <- matrix(stats::rnorm(150 * 5), 150, 5) \%*\% chol(0.4^abs(outer(1:5, 1:5, "-")))
colnames(x) <- paste0("V", 1:5)
bs <- net_boot(x, n_boot = 50, cores = 1)   # n_boot >= 1000 for real use
plot(difference_test(bs, type = "strength"))                   # box matrix
plot(difference_test(bs, type = "strength"), style = "forest") # forest plot
}
