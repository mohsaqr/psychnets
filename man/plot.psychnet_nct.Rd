% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.psychnet_nct}
\alias{plot.psychnet_nct}
\title{Plot a Network Comparison Test}
\usage{
\method{plot}{psychnet_nct}(x, type = c("strength", "structure", "edges"), alpha = 0.05, ...)
}
\arguments{
\item{x}{A \code{psychnet_nct} object from \code{\link[=net_compare]{net_compare()}}.}

\item{type}{One of \code{"strength"}, \code{"structure"}, \code{"edges"}.}

\item{alpha}{Significance level for colouring per-edge differences. Default
\code{0.05}.}

\item{...}{Unused.}
}
\value{
\code{x}, invisibly. Called for the plot it draws.
}
\description{
Visualises a \code{\link[=net_compare]{net_compare()}} result. \code{type = "strength"} (default) and
\code{type = "structure"} draw the permutation null distribution for the global
strength invariance (M) and the maximum edge-difference (S) statistics, with
the observed value and p-value marked; \code{type = "edges"} draws the observed
per-edge absolute differences, coloured by whether each edge differs
significantly.
}
\examples{
set.seed(1)
mk <- function(s) { set.seed(s)
  matrix(stats::rnorm(120 * 4), 120, 4) \%*\% chol(0.3^abs(outer(1:4, 1:4, "-"))) }
cmp <- net_compare(mk(1), mk(2), iter = 50)
plot(cmp)                  # global strength permutation null
plot(cmp, type = "edges")  # per-edge differences
}
